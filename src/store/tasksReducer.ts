

const initialState = {}

export const tasksReducer = (state: any = initialState, action: any) => {
    switch (action.type) {
        case 'XXX': {
            return state
        }
        default:
            return state
    }
}